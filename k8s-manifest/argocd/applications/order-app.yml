apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: product-service
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/ScaleWithAli/CloudAura-Azure
    targetRevision: main
    path: k8s-manifest/helm/microservice
    helm:
      valueFiles:
      - ../services/order-values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: product
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true 
